var table3excel = new Table2Excel()
table3excel.export(document.querySelectorAll("table"))

function Excel()
{
    var table3excel = new Table2Excel()
table3excel.export(document.querySelectorAll("table.table"))
}